# Fair_Face-AI_Model
FairVision is a deep learning–based CNN system built from scratch (without using a pre-trained model) to classify facial attributes—age, gender, and race—while applying bias mitigation techniques to improve fairness and performance on imbalanced datasets.
